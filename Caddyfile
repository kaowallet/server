# Caddy front for kao-proxy.
# TLS termination + compression only; all routing/upstream logic lives in kao-proxy.
# No access logs anywhere — privacy invariant.

{
	admin off
	email iamwehi@proton.me
}

api1.kaowallet.com {
	encode zstd gzip

	reverse_proxy kao-proxy:8080

	# Drop.
	handle {
		respond 404
	}
}
